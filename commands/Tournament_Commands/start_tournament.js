const { SlashCommandBuilder, ActionRowBuilder, ButtonBuilder, ButtonStyle, Component, EmbedBuilder } = require(`discord.js`);

module.exports = {
    data: new SlashCommandBuilder()
    .setName(`start_tournament`)
    .setDescription(`Coming Soon`),
    async execute (interaction) {

    }
}